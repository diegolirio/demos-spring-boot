# demos-spring-boot

#### First

Alterar a image do docker-compose para seu name no repo git.

```shell script
sh boot.sh
```

#### Second

```shell script
curl http://localhost:8082/

[
    {
        "firstname": "Nome",
        "lastname": "Ultimo nome"
    }
]

```

Se necessario retirar o "sudo" no boot.sh.
